<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BMI & Body Composition Calculator</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>BMI & Body Composition Calculator</h1>
        <form method="POST" action="/">
            <label>Weight (kg)</label>
            <input type="number" step="0.1" name="weight" placeholder="e.g. 70" required>

            <label>Height (meters)</label>
            <input type="number" step="0.01" name="height" placeholder="e.g. 1.75" required>

            <label>Age (years)</label>
            <input type="number" name="age" placeholder="e.g. 30" required>

            <label>Gender</label>
            <select name="gender" required>
                <option value="">Select</option>
                <option value="male">Male</option>
                <option value="female">Female</option>
            </select>

            <button type="submit">Calculate</button>
        </form>

        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>

        <% if (result) { %>
            <div class="result <%= result.categoryClass %>">
                <h3>Your Results</h3>
                <p><strong>BMI:</strong> <%= result.bmi.toFixed(2) %> <span>(<%= result.bmiCategory %>)</span></p>
                <p><strong>Body Fat (approx.):</strong> <%= result.bodyFat.toFixed(1) %>%</p>
                <p><strong>Lean Mass (approx.):</strong> <%= result.musclePercent.toFixed(1) %>%</p>

                <h4>Recommendations</h4>
                <ul>
                    <% result.recommendations.forEach(rec => { %>
                        <li><%= rec %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>
    </div>
</body>
</html>